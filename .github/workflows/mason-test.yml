name: mason test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  mason-test:
    runs-on: ubuntu-latest
    container:
      image: chapel/chapel:latest
    steps:
      - uses: actions/checkout@v4
      - name: Run `mason test`
        run: |
          mason test --parallel --show
